#
# Mongrels
#
check process mongrel_8100 with pidfile /var/www/apps/molten/current/log/mongrel.8100.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8100" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8100" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8101 with pidfile /var/www/apps/molten/current/log/mongrel.8101.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8101" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8101" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8102 with pidfile /var/www/apps/molten/current/log/mongrel.8102.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8102" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8102" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8103 with pidfile /var/www/apps/molten/current/log/mongrel.8103.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8103" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8103" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8104 with pidfile /var/www/apps/molten/current/log/mongrel.8104.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8104" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8104" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8105 with pidfile /var/www/apps/molten/current/log/mongrel.8105.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8105" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8105" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8106 with pidfile /var/www/apps/molten/current/log/mongrel.8106.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8106" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8106" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8107 with pidfile /var/www/apps/molten/current/log/mongrel.8107.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8107" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8107" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8108 with pidfile /var/www/apps/molten/current/log/mongrel.8108.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8108" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8108" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8109 with pidfile /var/www/apps/molten/current/log/mongrel.8109.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8109" as uid admin and gid admin
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8109" as uid admin and gid admin
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8110 with pidfile /var/www/apps/molten/current/log/mongrel.8110.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8110" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8110" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8111 with pidfile /var/www/apps/molten/current/log/mongrel.8111.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8111" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8111" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8112 with pidfile /var/www/apps/molten/current/log/mongrel.8112.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8112" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8112" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8113 with pidfile /var/www/apps/molten/current/log/mongrel.8113.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8113" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8113" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8114 with pidfile /var/www/apps/molten/current/log/mongrel.8114.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8114" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8114" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8115 with pidfile /var/www/apps/molten/current/log/mongrel.8115.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8115" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8115" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8116 with pidfile /var/www/apps/molten/current/log/mongrel.8116.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8116" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8116" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8117 with pidfile /var/www/apps/molten/current/log/mongrel.8117.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8117" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8117" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8118 with pidfile /var/www/apps/molten/current/log/mongrel.8118.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8118" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8118" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8119 with pidfile /var/www/apps/molten/current/log/mongrel.8119.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8119" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8119" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8120 with pidfile /var/www/apps/molten/current/log/mongrel.8120.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8120" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8120" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8121 with pidfile /var/www/apps/molten/current/log/mongrel.8121.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8121" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8121" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8122 with pidfile /var/www/apps/molten/current/log/mongrel.8122.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8122" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8122" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8123 with pidfile /var/www/apps/molten/current/log/mongrel.8123.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8123" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8123" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8124 with pidfile /var/www/apps/molten/current/log/mongrel.8124.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8124" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8124" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8125 with pidfile /var/www/apps/molten/current/log/mongrel.8125.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8125" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8125" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8126 with pidfile /var/www/apps/molten/current/log/mongrel.8126.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8126" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8126" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8127 with pidfile /var/www/apps/molten/current/log/mongrel.8127.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8127" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8127" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8128 with pidfile /var/www/apps/molten/current/log/mongrel.8128.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8128" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8128" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8129 with pidfile /var/www/apps/molten/current/log/mongrel.8129.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8129" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8129" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8130 with pidfile /var/www/apps/molten/current/log/mongrel.8130.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8130" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8130" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8131 with pidfile /var/www/apps/molten/current/log/mongrel.8131.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8131" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8131" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8132 with pidfile /var/www/apps/molten/current/log/mongrel.8132.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8132" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8132" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8133 with pidfile /var/www/apps/molten/current/log/mongrel.8133.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8133" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8133" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8134 with pidfile /var/www/apps/molten/current/log/mongrel.8134.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8134" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8134" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8135 with pidfile /var/www/apps/molten/current/log/mongrel.8135.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8135" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8135" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8136 with pidfile /var/www/apps/molten/current/log/mongrel.8136.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8136" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8136" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8137 with pidfile /var/www/apps/molten/current/log/mongrel.8137.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8137" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8137" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8138 with pidfile /var/www/apps/molten/current/log/mongrel.8138.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8138" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8138" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8139 with pidfile /var/www/apps/molten/current/log/mongrel.8139.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8139" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8139" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8140 with pidfile /var/www/apps/molten/current/log/mongrel.8140.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8140" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8140" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8141 with pidfile /var/www/apps/molten/current/log/mongrel.8141.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8141" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8141" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8142 with pidfile /var/www/apps/molten/current/log/mongrel.8142.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8142" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8142" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8143 with pidfile /var/www/apps/molten/current/log/mongrel.8143.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8143" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8143" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8144 with pidfile /var/www/apps/molten/current/log/mongrel.8144.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8144" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8144" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8145 with pidfile /var/www/apps/molten/current/log/mongrel.8145.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8145" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8145" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8146 with pidfile /var/www/apps/molten/current/log/mongrel.8146.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8146" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8146" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8147 with pidfile /var/www/apps/molten/current/log/mongrel.8147.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8147" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8147" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8148 with pidfile /var/www/apps/molten/current/log/mongrel.8148.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8148" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8148" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8149 with pidfile /var/www/apps/molten/current/log/mongrel.8149.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8149" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8149" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8150 with pidfile /var/www/apps/molten/current/log/mongrel.8150.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8150" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8150" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8151 with pidfile /var/www/apps/molten/current/log/mongrel.8151.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8151" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8151" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8152 with pidfile /var/www/apps/molten/current/log/mongrel.8152.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8152" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8152" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8153 with pidfile /var/www/apps/molten/current/log/mongrel.8153.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8153" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8153" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8154 with pidfile /var/www/apps/molten/current/log/mongrel.8154.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8154" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8154" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8155 with pidfile /var/www/apps/molten/current/log/mongrel.8155.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8155" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8155" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8156 with pidfile /var/www/apps/molten/current/log/mongrel.8156.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8156" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8156" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8157 with pidfile /var/www/apps/molten/current/log/mongrel.8157.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8157" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8157" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8158 with pidfile /var/www/apps/molten/current/log/mongrel.8158.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8158" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8158" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

check process mongrel_8159 with pidfile /var/www/apps/molten/current/log/mongrel.8159.pid
  start program = "/usr/local/bin/mongrel_rails cluster::start -C /etc/mongrel_cluster/molten.yml --clean --only 8159" $
  stop program = "/usr/local/bin/mongrel_rails cluster::stop  -C /etc/mongrel_cluster/molten.yml --clean --only 8159" a$
  if totalmem is greater than 110.0 MB for 2 cycles then restart      # eating up memory?
  if loadavg(5min) greater than 10 for 8 cycles then restart          # bad, bad, bad
  group molten

